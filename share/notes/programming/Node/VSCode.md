Using package-local TypeScript
==============================

In the root of the project, directory:

    mkdir .vscode

Then edit/create file `.vscode/settings.json` and add these lines:

    {
        "typescript.tsdk": "node_modules/typescript/lib"
    }

