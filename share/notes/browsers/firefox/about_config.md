# Tweaking the about:config

## Edit values:

    view_source.editor.external = true
    view_source.editor.path     = /usr/bin/gvim

## Add new value:

    browser.cache.disk.parent_directory = /tmp/ff-cache
