# Installation

## Copying the template file

    mkdir -p ~/.config/amkhlv/pandoc/
    cp template.html ~/.config/amkhlv/pandoc/

(The file `template.html` is based on the output of `pandoc --print-default-template=html`)

## Building and installing

    stack install

